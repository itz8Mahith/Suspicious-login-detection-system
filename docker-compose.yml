version: '3.8'

services:
  auth-sentinel:
    build: .
    container_name: auth-sentinel-soc
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
